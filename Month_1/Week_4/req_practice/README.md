# Quote Fetcher

Fetches a random quote using the DummyJSON API.

## Setup
1. Create virtual environment: `python -m venv .venv`
2. Activate it
3. Install dependencies: `pip install -r requirements.txt`
4. Run: `python fetch_quote.py`